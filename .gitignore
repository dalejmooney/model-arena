# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets. Added before any .env existed, deliberately: git only ignores files
# it is not already tracking, so ignoring after a commit does nothing.
.env
.env.*
!.env.example
